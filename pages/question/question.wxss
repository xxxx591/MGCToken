/* pages/question/question.wxss */

.submit {
  position: fixed;
  left: 0;
  bottom: 0;
  width: 100%;
  height: 94rpx;
  display: flex;
  color: #fff;
  align-items: center;
  text-align: center;
  margin-top: 50rpx;
}

.submit text {
  z-index: 999;
  width: 100%;
  line-height: 94rpx;
  color: #fff;
  background: #262525;
}

.submit image {
  position: absolute;
  left: 30rpx;
  top: 0;
  width: 690rpx;
  height: 94rpx;
}

.box {
  background: #fff;
  padding: 30rpx;
}

.box view {
  background: #fff;
  font-size: 34rpx;
  line-height: 45rpx;
}

.submit {
  position: fixed;
  left: 0;
  bottom: 0;
  width: 100%;
  height: 94rpx;
  display: flex;
  background-image: var(--gradualBlue);
  color: var(--white);
  align-items: center;
  text-align: center;
  margin-top: 50rpx;
}

.submit text {
  z-index: 999;
  width: 100%;
  line-height: 94rpx;
  background-image: var(--gradualBlue);
  color: var(--white);
}

.btn {
  margin: 0 auto;
  width: 40%;
  height: 94rpx;
  display: flex;
  background-image: var(--gradualBlue);
  color: var(--white);
  align-items: center;
  text-align: center;
  margin-top: 50rpx;
  border-radius: 6px;
}
.btn text {
  z-index: 999;
  width: 100%;
  line-height: 94rpx;
  background-image: var(--gradualBlue);
  color: var(--white);
}